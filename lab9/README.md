# Лабораторная работа №9: Анализ шума

(большой `sweden.wav` заменен на короткий файл `short.wav`, чтобы результат помещался в лимиты GitHub)

## Что сделано
1. Загружается подготовленная запись `short.wav`
2. Учитывается число каналов: исходный файл стерео, для анализа строится моно-сигнал как среднее каналов, а очищенный звук сохраняется снова в стерео
3. Строятся спектрограммы до и после обработки
4. Шум оценивается по участкам с малой энергией
5. Выполняется шумопонижение методом спектрального вычитания.
6. Восстанавливается звуковая дорожка `short_denoised.wav`
7. Находятся области максимальной энергии при `delta t = 0.1 c` и `delta f = 50 Гц`

## Формулы и параметры
Для спектрограммы используется оконное преобразование Фурье:

```text
STFT{x[n]}(m, w) = sum x[n]w[n - m]e^(-jwn)
spectrogram{x[n]} = |STFT{x[n]}|^2
```

Окно Ханна:

```text
w(n) = 0.5 - 0.5 * cos(2*pi*n / (N - 1))
```

Параметры:

```text
длина окна = 50 мс
перекрытие = 75%
шкала частот на спектрограмме = логарифмическая
```

Метод спектрального вычитания:

```text
Y[f, t] = max(X[f, t] - kW[f, t], 0)
```

где:
- `X[f, t]` — амплитудный спектр исходного сигнала
- `W[f, t]` — оценка амплитудного спектра шума
- `Y[f, t]` — амплитудный спектр после очистки
- фаза берется от исходного сигнала
- затем выполняется обратное STFT

В программе:

```text
k = 1.6
нижняя граница усиления = 0.08
```

Нижняя граница усиления оставляет небольшую часть шума и уменьшает эффект "музыкального шума"

## Структура
- `main.py` — код лабораторной
- `short.wav` — исходная запись
- `output/spectrograms/short_before.png` — спектрограмма до обработки
- `output/spectrograms/short_after.png` — спектрограмма после обработки
- `output/audio/short_denoised.wav` — восстановленная звуковая дорожка
- `output/tables/energy_maxima.csv` — таблица максимумов энергии
- `output/reports/report.txt` — краткий отчет

## Запуск
Из директории `lab9`:

```bash
python main.py
```

С явным указанием входного файла и папки результатов:

```bash
python main.py --input ./short.wav --output-dir ./output
```

## Полученные результаты
Исходная запись `short.wav`:

```text
частота дискретизации = 44100 Гц
количество каналов = 2
длительность = 30.14 с
тип отсчетов = int16
```

Для анализа спектрограмм стереозапись была переведена в моно как среднее двух каналов. При восстановлении звука оба канала обрабатывались отдельно, поэтому файл `short_denoised.wav` сохранен как стерео.

Оценка уровня шума:

```text
RMS всего сигнала = 0.151751
RMS шума до обработки = 0.006724
RMS шума после обработки = 0.004241
ОСШ до обработки = 27.07 дБ
ОСШ после обработки = 31.07 дБ
```

После спектрального вычитания оцененный уровень шума стал меньше, а отношение сигнал/шум увеличилось примерно на `4.00 дБ`.

Моменты и частотные области с наибольшей энергией при `delta t = 0.1 c` и `delta f = 50 Гц`:

| № | Время, с | Частота, Гц | Энергия |
|---|---:|---:|---:|
| 1 | 22.5-22.6 | 350-400 | 0.765018 |
| 2 | 22.6-22.7 | 350-400 | 0.690816 |
| 3 | 22.7-22.8 | 350-400 | 0.680580 |
| 4 | 9.9-10.0 | 350-400 | 0.679581 |
| 5 | 22.8-22.9 | 350-400 | 0.640226 |
| 6 | 11.1-11.2 | 400-450 | 0.632914 |
| 7 | 11.2-11.3 | 400-450 | 0.577342 |
| 8 | 8.9-9.0 | 400-450 | 0.570566 |
| 9 | 10.0-10.1 | 350-400 | 0.568040 |
| 10 | 22.9-23.0 | 350-400 | 0.564387 |
| 11 | 2.5-2.6 | 400-450 | 0.559346 |
| 12 | 2.7-2.8 | 400-450 | 0.553265 |

По таблице видно, что самые сильные области находятся в диапазонах примерно `350-450 Гц`, что соответствует основным тонам музыкального сигнала.
